#
# Ansible Playbook: create or delete A record in AWS Route53
#
---
- hosts: localhost
  gather_facts: no
  connection: local
  tasks:
    - name: Set public dns A record in Route53
      route53:
        type: A
        command: "{{ command }}"
        zone: "{{ zone }}"
        record: "{{ record }}"
        value: "{{ value }}"
        ttl: "{{ ttl }}"
